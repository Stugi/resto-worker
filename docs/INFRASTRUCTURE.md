# RestoWorker — Инфраструктура и технологии

## Обзор архитектуры

```
Telegram ←→ Vercel (Nuxt 4 / Nitro) ←→ Neon (PostgreSQL)
                    ↕                         ↕
               OpenAI API              Prisma ORM
                    ↕
              Vercel Cron / Внешний cron
```

---

## 1. Хостинг и деплой

### Vercel (Hobby) — Frontend + API + SSR
- **Тариф:** Бесплатно (Pro — $20/мес)
- ⚠️ Timeout 10сек на Hobby (Pro: 60сек)
- ⚠️ Серверы за рубежом — риск 152-ФЗ
- ⚠️ Может быть заблокирован в РФ
- ⚠️ Оплата Pro только иностранной картой

### Домен — resto-worker.vercel.app
- **Тариф:** Бесплатно
- ⚠️ Субдомен Vercel — непрофессионально для клиентов
- Свой домен ~500₽/год на Рег.ру

---

## 2. База данных

### Neon — PostgreSQL
- **Тариф:** Free: 0.5GB, 191 compute-hours. Платный $19/мес за 10GB
- ⚠️ **0.5GB мало** — 10 ресторанов за ~6 мес упрутся в лимит
- ⚠️ Cold start 1-3сек после простоя
- ⚠️ Серверы в США — latency ~150мс из РФ
- ⚠️ 152-ФЗ — персональные данные хранятся за рубежом

### Prisma — ORM
- ⚠️ Тяжёлый холодный старт на serverless (инициализация Prisma Client при каждом вызове)

---

## 3. AI / ML

### OpenAI Whisper API — транскрипция голоса
- **Тариф:** $0.006/мин аудио
- ⚠️ Timeout 10сек на Hobby может не хватить для аудио >30сек
- ⚠️ Оплата иностранной картой
- ⚠️ Нужен баланс на счёте (budget != credit)

### OpenAI GPT-4o-mini — генерация отчётов
- **Тариф:** ~$0.01-0.03 за отчёт
- ⚠️ Timeout может не хватить на Hobby
- ⚠️ Оплата иностранной картой

---

## 4. Telegram

### Grammy + Bot API — бот (webhook)
- **Тариф:** Бесплатно
- ✅ Стабильно работает на serverless
- Webhook на `/api/bot/webhook`

### GramJS — userbot (MTProto)
- **Тариф:** Бесплатно
- ⚠️ Требует постоянного соединения — плохо совместим с serverless
- ⚠️ Сессия может слетать между вызовами

---

## 5. Планировщик (Cron)

### Vercel Cron — автоотчёты
- **Тариф:** Бесплатно
- ⚠️ Hobby: макс. 1 раз/день, точность ±59 мин
- ⚠️ Для частых запусков нужен Pro ($20/мес) или внешний cron-сервис

### Альтернативы (бесплатные)
- **cron-job.org** — 60 заданий, от 1 мин
- **EasyCron** — 1 задание, от 20 мин
- **UptimeRobot** — 50 мониторов, от 5 мин

---

## 6. Фреймворк и библиотеки

- **Nuxt 4** (Vue 3, Composition API) — frontend ✅
- **Nitro** — серверный движок ✅
- **shadcn-vue** — UI компоненты ✅
- **Tailwind CSS** — стили ✅
- **Pinia** — стейт-менеджер ✅
- **@paralleldrive/cuid2** — генерация ID ✅

---

## 7. Риски

### 🔴 Высокий

**Vercel Hobby timeout 10сек**
- Whisper (10-30сек) + GPT (5-15сек) могут не уложиться
- Решение: Pro ($20/мес) даёт 60сек. Или VPS

**Cron раз в день**
- Невозможно отправлять отчёты в произвольное время для разных ресторанов
- Решение: внешний cron (cron-job.org) каждые 15 мин. Или Pro

**OpenAI оплата**
- Нужна иностранная карта с балансом
- Альтернатива: self-hosted Whisper, но нужен GPU

### 🟡 Средний

**Neon cold start**
- Первый запрос после простоя 1-3сек
- Решение: pooler URL + keepalive. Или managed PostgreSQL

**Userbot на serverless**
- MTProto сессия может слетать
- Решение: вынести на VPS с постоянным процессом

**152-ФЗ**
- Персональные данные сотрудников хранятся за рубежом
- Решение: серверы в РФ (Timeweb, Selectel, Yandex Cloud)

**Блокировка Vercel в РФ**
- Весь сервис ляжет
- Решение: VPS в РФ + Docker

**Нет своего домена**
- vercel.app выглядит непрофессионально
- Решение: домен на Рег.ру ~500₽/год

### 🟢 Низкий

- **Nuxt / Vue / Tailwind** — стабильные, зрелые технологии
- **Grammy Bot API** — стабильно работает на serverless
- **Prisma** — поддерживает все основные PostgreSQL хосты

---

## 8. Миграция на продакшен (РФ)

### Вариант 1: VPS (рекомендуется)

- **Nuxt (Docker)** → Timeweb Cloud VPS — от 300₽/мес
- **PostgreSQL** → Timeweb Managed DB — от 300₽/мес
- **Домен** → Рег.ру — ~500₽/год
- **Cron** → node-cron в Docker — бесплатно
- **OpenAI** → без изменений — pay-as-you-go
- **Итого: ~600-800₽/мес**

### Вариант 2: Vercel Pro (быстрее, без миграции)

- **Vercel Pro** — $20/мес (~1800₽)
- **Neon Pro** или Supabase — бесплатно / от $25/мес
- **Домен** → покупка отдельно — ~500₽/год
- **Итого: ~$20-45/мес**

### Перенос базы (в любом варианте)

```bash
# Экспорт из Neon
pg_dump "postgresql://...@neon.tech/neondb" > backup.sql

# Импорт в новую БД
psql "postgresql://user:pass@new-host/dbname" < backup.sql

# Обновить DATABASE_URL в .env / хостинге
```

---

## 9. Переменные окружения

Все хранятся в **Vercel → Settings → Environment Variables**:

- `DATABASE_URL` — Prisma → Neon
- `TELEGRAM_BOT_TOKEN` — Grammy бот
- `TELEGRAM_BOT_USERNAME` — логин-виджет (public)
- `OPENAI_API_KEY` — Whisper + GPT
- `SESSION_SECRET` — сессии Nitro
- `CRON_SECRET` — авторизация cron-запросов
- `TELEGRAM_API_ID` — MTProto userbot
- `TELEGRAM_API_HASH` — MTProto userbot
- `TELEGRAM_SESSION` — MTProto сессия
